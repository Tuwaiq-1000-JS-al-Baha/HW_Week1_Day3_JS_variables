function calcCircumfrence(radius) {
  var y = (Math.PI * radius) * 2;
  console.log("The cricumfrence is " +y +".");
  }
  function calcArea(radius) {
      var x = (Math.PI * radius) * radius;
      console.log("The area is " + x +".");
  }

  calcCircumfrence(30);
  calcArea(10);
  
}